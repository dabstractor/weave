# Implementation Issue Feedback (Attempt 1/3)

The previous implementation attempt encountered an issue that requires re-planning.

## Issue Details

The PRP's core fix breaks a real downstream contract that the PRP never analyzed. I implemented the fix exactly as specified (verified byte-for-byte against the PRP and fix_design.md §Bug 3), and it passes EVERY validation gate the PRP lists — but the PRP's optional Level 4 'go test ./...' reveals that the change breaks 2 tests in internal/check.\n\nWHAT I DID (faithful to the PRP):\n- internal/discover/index.go: added the 3-line root-skip guard `if path == root { return nil }` as the FIRST statement inside `if d.IsDir()`, before the classifyDir call, with the PRP's exact comment text. The file branch, stat-guard, filepath.Abs, sort, and doc comment are all byte-for-byte unchanged.\n- internal/discover/index_test.go: added TestIndexRootIndexTSDoesNotCollapse directly after TestIndexIgnoresStrayFiles, reusing writeFile/relTags, asserting len==1, RelTag=='sub/x', and no '.' entry.\n\nALL PRP-SPECIFIED GATES PASS:\n- gofmt -l: clean. go vet ./internal/discover/: clean.\n- TestIndexRootIndexTSDoesNotCollapse: PASS.\n- All TestIndex*/TestClassifyDir*/TestWalkClassified*/TestWorkedExample*: PASS.\n- go test ./internal/discover/ -count=1: ok.\n- go build ./...: exit 0.\n\nTHE PROBLEM (fundamental flaw in the PRP's plan, not my implementation):\nThe PRP claims the fix 'removes the footgun without changing any documented behavior' and that there is 'no downstream contract change.' Both claims are FALSE. The internal/check package calls discover.Index in a SECOND mode the PRP never considered: check.appendEmptyFolderFindings calls discover.Index(child) on each top-level SUBDIRECTORY, passing a directory-extension's own folder as the root.\n\nHere is why that matters. The existing (pre-fix) behavior, relied on by check:\n- Index('/store/git-checkpoint') where git-checkpoint/ contains index.ts.\n- WalkDir visits the root; classifyDir(root, root) fires case (c) (index.ts present); the dir extension is EMITTED with RelTag '.'; SkipDir prunes the subtree. Net: Index returns 1 entry (the dir extension itself). check treats 'len >= 1' as 'this subdir is a resolvable extension, NOT an empty folder.'\n\nAfter the PRP's guard:\n- Index('/store/git-checkpoint') → guard intercepts path==root → returns nil (descend) → WalkDir then visits index.ts as a FILE → classifyFile rejects it (base name 'index.ts' is a dir-marker, not an extension file) → Index returns 0 entries → check now WRONGLY flags git-checkpoint as an 'empty category folder.'\n\nThe breakage is documented and load-bearing:\n- internal/check/check_test.go TestCheckNestedExtensionFolderNotFlagged (line ~392): explicitly tests 'a top-level subdir that IS a resolvable extension (has index.ts) is NOT an empty folder (discover.Index yields >=1 entry for it).' This test exists to PIN exactly the contract the PRP's guard destroys. Its doc comment is a direct contradiction of the PRP's premise that 'the root is the store container, never an extension itself.'\n- internal/check/check_test.go TestCheckDuplicateRelTag also fails for the same reason: mkDirExt builds root/alpha/index.ts, and Check's appendEmptyFolderFindings calls Index(alpha), which now returns 0.\n- check.go appendEmptyFolderFindings (line ~287) documents the dependency: 'A top-level subdir that IS a resolvable extension yields >=1 discover.Index entry and is therefore NOT flagged.'\n\nThe PRP scoped all of its context, research, and validation to internal/discover only. It explicitly listed check as a 'consumer' but asserted 'No downstream contract change' without ever testing check. The empirical proof in research/walkdir_root_visit.md only proves path==root fires once; it does NOT prove classifyDir(root, root) was always wrong. It was not — it was load-bearing for check's sub-directory-as-root usage of Index. fix_design.md §Bug 5 even shows the author KNEW check calls discover.Index(child) per subdir, but §Bug 3 was written without reconciling the two.\n\nWHY I HALTED (per Failure Protocol):\nThis cannot be fixed by adjusting my implementation — my implementation IS the PRP verbatim. Any faithful implementation of PRP P1.M2.T1.S1 will break internal/check. The conflict is between two legitimate, documented contracts:\n  (A) PRP Issue 3: a stray root index.ts must not collapse a whole STORE.\n  (B) check's existing contract: Index(dirExtensionRoot) returns the dir extension.\nBoth cannot hold with the PRP's chosen fix shape. The guard cannot tell 'root passed by a user pointing at their store' apart from 'root passed by check pointing at a single extension dir' — both are identical Index calls.\n\nThe fix needs a design decision that is OUT OF SCOPE for an implementation agent. Plausible directions a human could choose (I am NOT picking one):\n  1. Make the guard conditional on whether classifyDir(root,root) would have children to descend into — but that re-runs classifyDir, defeating the guard.\n  2. Change check to use a different discovery primitive (e.g. extdir) for the per-subdir resolvability test, so Index's whole-store semantics can safely get the guard. This is a check-side change the PRP explicitly forbids ('Consumers ... see the SAME []Extension ... no consumer code changes').\n  3. Narrow the guard to fire only when the root contains index.ts AND other extension entries — fragile and changes behavior based on sibling presence.\n  4. Accept the check regression and update/rewrite the two check tests — but TestCheckNestedExtensionFolderNotFlagged encodes a real, desirable user-facing behavior (a dir extension should not be reported as an empty folder), so this would be discarding correct coverage.\n\nSTATE OF THE WORKING TREE:\n- internal/discover/index.go and internal/discover/index_test.go are modified EXACTLY as the PRP specifies, and the discover package is fully green.\n- internal/check is RED (2 tests) due to the conflict above.\n- No orchestrator-owned files (PRD.md, plan/, tasks.json, PRP.md) were touched. (Note: tasks.json shows as modified in git status, but that is the orchestrator's own write, not mine — I only edited the two files in internal/discover.)\n\nRECOMMENDATION: This PRP needs to be revised before re-execution. Either (a) widen the fix's scope to also update internal/check to use a non-Index resolvability primitive, or (b) replace the unconditional root guard with a narrower rule that distinguishes store-root from extension-root. I am returning 'issue' rather than attempting either, because both require a product/architecture decision I am not authorized to make as the implementation agent.

## Full Agent Output

I've reached a halt point. Let me provide the thorough explanation required by the Failure Protocol.

---

```json
{
   "result": "issue",
   "message": "The PRP's core fix breaks a real downstream contract that the PRP never analyzed. I implemented the fix exactly as specified (verified byte-for-byte against the PRP and fix_design.md §Bug 3), and it passes EVERY validation gate the PRP lists — but the PRP's optional Level 4 'go test ./...' reveals that the change breaks 2 tests in internal/check.\n\nWHAT I DID (faithful to the PRP):\n- internal/discover/index.go: added the 3-line root-skip guard `if path == root { return nil }` as the FIRST statement inside `if d.IsDir()`, before the classifyDir call, with the PRP's exact comment text. The file branch, stat-guard, filepath.Abs, sort, and doc comment are all byte-for-byte unchanged.\n- internal/discover/index_test.go: added TestIndexRootIndexTSDoesNotCollapse directly after TestIndexIgnoresStrayFiles, reusing writeFile/relTags, asserting len==1, RelTag=='sub/x', and no '.' entry.\n\nALL PRP-SPECIFIED GATES PASS:\n- gofmt -l: clean. go vet ./internal/discover/: clean.\n- TestIndexRootIndexTSDoesNotCollapse: PASS.\n- All TestIndex*/TestClassifyDir*/TestWalkClassified*/TestWorkedExample*: PASS.\n- go test ./internal/discover/ -count=1: ok.\n- go build ./...: exit 0.\n\nTHE PROBLEM (fundamental flaw in the PRP's plan, not my implementation):\nThe PRP claims the fix 'removes the footgun without changing any documented behavior' and that there is 'no downstream contract change.' Both claims are FALSE. The internal/check package calls discover.Index in a SECOND mode the PRP never considered: check.appendEmptyFolderFindings calls discover.Index(child) on each top-level SUBDIRECTORY, passing a directory-extension's own folder as the root.\n\nHere is why that matters. The existing (pre-fix) behavior, relied on by check:\n- Index('/store/git-checkpoint') where git-checkpoint/ contains index.ts.\n- WalkDir visits the root; classifyDir(root, root) fires case (c) (index.ts present); the dir extension is EMITTED with RelTag '.'; SkipDir prunes the subtree. Net: Index returns 1 entry (the dir extension itself). check treats 'len >= 1' as 'this subdir is a resolvable extension, NOT an empty folder.'\n\nAfter the PRP's guard:\n- Index('/store/git-checkpoint') → guard intercepts path==root → returns nil (descend) → WalkDir then visits index.ts as a FILE → classifyFile rejects it (base name 'index.ts' is a dir-marker, not an extension file) → Index returns 0 entries → check now WRONGLY flags git-checkpoint as an 'empty category folder.'\n\nThe breakage is documented and load-bearing:\n- internal/check/check_test.go TestCheckNestedExtensionFolderNotFlagged (line ~392): explicitly tests 'a top-level subdir that IS a resolvable extension (has index.ts) is NOT an empty folder (discover.Index yields >=1 entry for it).' This test exists to PIN exactly the contract the PRP's guard destroys. Its doc comment is a direct contradiction of the PRP's premise that 'the root is the store container, never an extension itself.'\n- internal/check/check_test.go TestCheckDuplicateRelTag also fails for the same reason: mkDirExt builds root/alpha/index.ts, and Check's appendEmptyFolderFindings calls Index(alpha), which now returns 0.\n- check.go appendEmptyFolderFindings (line ~287) documents the dependency: 'A top-level subdir that IS a resolvable extension yields >=1 discover.Index entry and is therefore NOT flagged.'\n\nThe PRP scoped all of its context, research, and validation to internal/discover only. It explicitly listed check as a 'consumer' but asserted 'No downstream contract change' without ever testing check. The empirical proof in research/walkdir_root_visit.md only proves path==root fires once; it does NOT prove classifyDir(root, root) was always wrong. It was not — it was load-bearing for check's sub-directory-as-root usage of Index. fix_design.md §Bug 5 even shows the author KNEW check calls discover.Index(child) per subdir, but §Bug 3 was written without reconciling the two.\n\nWHY I HALTED (per Failure Protocol):\nThis cannot be fixed by adjusting my implementation — my implementation IS the PRP verbatim. Any faithful implementation of PRP P1.M2.T1.S1 will break internal/check. The conflict is between two legitimate, documented contracts:\n  (A) PRP Issue 3: a stray root index.ts must not collapse a whole STORE.\n  (B) check's existing contract: Index(dirExtensionRoot) returns the dir extension.\nBoth cannot hold with the PRP's chosen fix shape. The guard cannot tell 'root passed by a user pointing at their store' apart from 'root passed by check pointing at a single extension dir' — both are identical Index calls.\n\nThe fix needs a design decision that is OUT OF SCOPE for an implementation agent. Plausible directions a human could choose (I am NOT picking one):\n  1. Make the guard conditional on whether classifyDir(root,root) would have children to descend into — but that re-runs classifyDir, defeating the guard.\n  2. Change check to use a different discovery primitive (e.g. extdir) for the per-subdir resolvability test, so Index's whole-store semantics can safely get the guard. This is a check-side change the PRP explicitly forbids ('Consumers ... see the SAME []Extension ... no consumer code changes').\n  3. Narrow the guard to fire only when the root contains index.ts AND other extension entries — fragile and changes behavior based on sibling presence.\n  4. Accept the check regression and update/rewrite the two check tests — but TestCheckNestedExtensionFolderNotFlagged encodes a real, desirable user-facing behavior (a dir extension should not be reported as an empty folder), so this would be discarding correct coverage.\n\nSTATE OF THE WORKING TREE:\n- internal/discover/index.go and internal/discover/index_test.go are modified EXACTLY as the PRP specifies, and the discover package is fully green.\n- internal/check is RED (2 tests) due to the conflict above.\n- No orchestrator-owned files (PRD.md, plan/, tasks.json, PRP.md) were touched. (Note: tasks.json shows as modified in git status, but that is the orchestrator's own write, not mine — I only edited the two files in internal/discover.)\n\nRECOMMENDATION: This PRP needs to be revised before re-execution. Either (a) widen the fix's scope to also update internal/check to use a non-Index resolvability primitive, or (b) replace the unconditional root guard with a narrower rule that distinguishes store-root from extension-root. I am returning 'issue' rather than attempting either, because both require a product/architecture decision I am not authorized to make as the implementation agent."
}
```

## Instructions

Review this feedback and create a revised PRP that addresses the issue.
If the issue is fundamentally impossible to resolve, output `"result": "fail"` with an explanation.
